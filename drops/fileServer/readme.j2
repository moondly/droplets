###################################
## DROPLET {{drop_name}}
###################################

Files to serve must be mapped under: /var/files

To access the files use: http://fileserver:8000

To connect:

{% for host in ansible_play_hosts | sort %}
  ssh root@{{ hostvars[host].ansible_hostname }}   ({{ hostvars[host].inventory_hostname }})
{% endfor %}

------
